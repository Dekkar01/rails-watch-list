<div class="container">
  <h1>New review</h1>
  <%= simple_form_for (@lists) do |f| %>
    <%= f.input :name %>
    <%= f.submit "Create new list", class: "btn btn-primary" %>
  <% end %>
  <%= link_to "Back", root_path %>
</div>
